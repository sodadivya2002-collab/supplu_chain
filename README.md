# Supply Chain Chatbot

An AI-powered chatbot for supply chain operations (orders, inventory, shipments, documents).

## Features
- Natural language queries
- Document processing (DOCX)
- Snowflake integration
- Streamlit interface

## How to run locally
```bash
pip install -r requirements.txt
streamlit run app/app.py
